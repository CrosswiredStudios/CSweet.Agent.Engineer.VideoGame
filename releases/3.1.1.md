# 3.1.1

Expose the coding-harness token budgets as installation configuration. `maxContextWindowTokens` documents the selected model's planning ceiling and `maxOutputTokens` caps each Engineer model response, replacing the previously hardcoded 128,000 / 16,000 values. Both settings are required and validated so the output budget stays below the context window. Also fix the stale implementation identity left behind by the 3.1.0 version bump.
